#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if(a%15==0)
    {
        printf("LearningSaga-ILEARN");
    }
    else if(a%5==0)
    {
        printf("LearningSaga");
    }
    else if(a%3==0)
    {
        printf("ILEARN");
    }
    
    
}
