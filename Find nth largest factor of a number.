#include<stdio.h>
int main()
{
    int a,b,count=0;
    scanf("%d %d",&a,&b);
    for(int i=a;i>=1;i--)
    {
        if(a%i==0)
        {
            count++;
            if(count==b)
            {
                printf("%d",i);
                break;
            }
        }
    }
}
