#include<stdio.h>
int main()
{
   int a,b,c,count=0;
   scanf("%d %d %d",&a,&b,&c);
   for(int i=1;i<=a;i++)
   {
       if((a%i==0)&&(b%i==0))
       {
           count=count+1;
       }
       if(count==c)
       {
           printf("%d",i);
           break;
       }
   }
}
