#include<stdio.h>
int main()
{
    int num,years,weeks,days;
    scanf("%d",&num);
    years=num/365;
    weeks=(num%365)/7;
    days=(num%365)%7;
    printf("Years: %d\nWeeks: %d\nDays: %d",years,weeks,days);
}
