#include<stdio.h>
int main()
{
    int a,b,c,i,count;
    scanf("%d %d %d",&a,&b,&c);
    for(i=a;i<=b;i++)
    {
        count=0;
        for(int j=1;j<=b;j++)
        {
            if(i%j==0)
            {
                count++;
            }
           
        }
         if(count==c)
            {
                printf("%d ",i);
            }
    }
}
