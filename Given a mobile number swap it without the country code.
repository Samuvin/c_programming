#include<stdio.h>
int main()
{
    long long int num,first=0,last=0,num2;
    scanf("%lld",&num);
    while(num>0)
    {
        num=num%10000000000;
        first=num/100000;
        last=num%100000;
        break;
    }
    printf("%lld%lld",last,first);
}
