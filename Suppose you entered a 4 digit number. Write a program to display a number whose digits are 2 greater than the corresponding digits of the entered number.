#include<stdio.h>
int main()
{
    int a,e,i,o,u;
    scanf("%d",&a);
    e=(((a/1000)%10)+2)%10;
    i=(((a/100)%10)+2)%10;
    o=(((a/10)%10)+2)%10;
    u=((a%10)+2)%10;
    printf("%d%d%d%d",e,i,o,u);
}
